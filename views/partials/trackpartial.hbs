<div class="track-card">
  <p>{{name}}</p>
  <div class="track-autio">
    <audio controls src="{{this.preview_url}}">
      Your browser does not support the
      <code>audio</code>
      element.
    </audio>
  </div>
</div>
